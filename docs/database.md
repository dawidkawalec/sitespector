# SiteSpector - Database Schema & Management

## Overview

SiteSpector uses a **dual-database** architecture:

| Database | Host | Purpose |
|---|---|---|
| **VPS PostgreSQL 16** | Docker (`sitespector-postgres`) | Audits, tasks, schedules, chat, public forms |
| **Supabase PostgreSQL** | Supabase cloud | Auth, profiles, workspaces, projects, billing |

Cross-database references (e.g. `audits.project_id` on VPS pointing to `projects.id` in Supabase) are **UUID-based with no foreign key** -- integrity is enforced in the application layer.

**ORM**: SQLAlchemy 2.0 (async) with asyncpg driver
**Migrations**: Alembic (18 migration files in `backend/alembic/versions/`)

---

## VPS Database Connection

**Container**: `sitespector-postgres` (image: `postgres:16-alpine`)
**Port**: 5432 (internal only)
**URL**: `postgresql+asyncpg://sitespector_user:sitespector_password@postgres:5432/sitespector_db`

**Connection pool** (configured in `backend/app/database.py`):

| Setting | Value |
|---|---|
| pool_size | 10 |
| max_overflow | 20 |
| pool_pre_ping | true |
| pool_recycle | 3600s |

---

## VPS PostgreSQL Tables

All models live in `backend/app/models.py`.

### 1. users

User accounts and legacy authentication.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK, default uuid4 |
| email | VARCHAR(255) | UNIQUE, NOT NULL, indexed |
| password_hash | VARCHAR(255) | NOT NULL |
| subscription_tier | ENUM (free/pro/enterprise) | NOT NULL, default 'free', indexed |
| stripe_customer_id | VARCHAR(255) | nullable |
| audits_count | INTEGER | NOT NULL, default 0 |
| created_at | TIMESTAMPTZ | NOT NULL, server default now(), indexed |
| updated_at | TIMESTAMPTZ | NOT NULL, server default now(), auto-update |

**Relationships**: `users` 1--N `audits` (cascade delete-orphan)

---

### 2. audits

Website audit records with scores and full JSONB results.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK |
| user_id | UUID | FK -> users.id, nullable (legacy), indexed |
| workspace_id | UUID | nullable, indexed (Supabase workspace, no FK) |
| project_id | UUID | nullable, indexed (Supabase project, no FK) |
| url | VARCHAR(2048) | NOT NULL |
| status | ENUM (pending/processing/completed/failed) | NOT NULL, default 'pending', indexed |
| overall_score | FLOAT | nullable |
| seo_score | FLOAT | nullable |
| performance_score | FLOAT | nullable |
| content_score | FLOAT | nullable |
| is_local_business | BOOLEAN | NOT NULL, default false |
| results | JSONB | nullable (full audit data) |
| pdf_url | VARCHAR(2048) | nullable |
| processing_step | VARCHAR(100) | nullable (progress bar label) |
| error_message | TEXT | nullable |
| processing_logs | JSONB | nullable, default [] |
| ai_status | VARCHAR(20) | nullable (processing/completed/failed) |
| senuto_country_id | INTEGER | nullable, default 200 |
| senuto_fetch_mode | VARCHAR(20) | nullable, default 'subdomain' |
| run_ai_pipeline | BOOLEAN | NOT NULL, default true |
| run_execution_plan | BOOLEAN | NOT NULL, default true |
| execution_plan_status | VARCHAR(20) | nullable (processing/completed/failed) |
| crawler_user_agent | VARCHAR(500) | nullable |
| crawl_blocked | BOOLEAN | NOT NULL, default false |
| created_at | TIMESTAMPTZ | NOT NULL, indexed |
| started_at | TIMESTAMPTZ | nullable |
| completed_at | TIMESTAMPTZ | nullable |
| rag_indexed_at | TIMESTAMPTZ | nullable, indexed |

**Relationships**: `audits` 1--N `competitors` (cascade), `audits` 1--N `audit_tasks` (cascade)

---

### 3. competitors

Competitor URLs linked to an audit for comparative Lighthouse analysis.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK |
| audit_id | UUID | FK -> audits.id, NOT NULL, indexed |
| url | VARCHAR(2048) | NOT NULL |
| status | ENUM (pending/completed/failed) | NOT NULL, default 'pending' |
| results | JSONB | nullable (Lighthouse data) |
| created_at | TIMESTAMPTZ | NOT NULL |

---

### 4. audit_tasks

Execution plan items generated by the AI pipeline.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK |
| audit_id | UUID | FK -> audits.id, NOT NULL, indexed |
| module | VARCHAR(50) | NOT NULL, indexed (seo/performance/visibility/ai_overviews/links/images/ux/security) |
| title | VARCHAR(500) | NOT NULL |
| description | TEXT | NOT NULL |
| category | VARCHAR(50) | NOT NULL (technical/content/offsite/ux/security) |
| priority | ENUM (critical/high/medium/low) | NOT NULL, indexed |
| impact | VARCHAR(20) | NOT NULL (high/medium/low) |
| effort | VARCHAR(20) | NOT NULL (easy/medium/hard) |
| is_quick_win | BOOLEAN | NOT NULL, default false, indexed |
| fix_data | JSONB | nullable (`{current_value, suggested_value, code_snippet}`) |
| status | ENUM (pending/done) | NOT NULL, default 'pending', indexed |
| notes | TEXT | nullable |
| source | VARCHAR(50) | NOT NULL (execution_plan/ai_context) |
| sort_order | INTEGER | NOT NULL, default 0 |
| created_at | TIMESTAMPTZ | NOT NULL, indexed |
| updated_at | TIMESTAMPTZ | NOT NULL, auto-update |
| completed_at | TIMESTAMPTZ | nullable |

---

### 5. audit_schedules

Recurring audit configuration.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK |
| user_id | UUID | FK -> users.id, NOT NULL, indexed |
| workspace_id | UUID | NOT NULL, indexed |
| project_id | UUID | nullable, indexed |
| url | VARCHAR(2048) | NOT NULL |
| frequency | ENUM (daily/weekly/monthly) | NOT NULL |
| is_active | BOOLEAN | NOT NULL, default true |
| include_competitors | BOOLEAN | default true |
| competitors_urls | JSONB | nullable (list of URL strings) |
| last_run_at | TIMESTAMPTZ | nullable |
| next_run_at | TIMESTAMPTZ | nullable, indexed |
| created_at | TIMESTAMPTZ | NOT NULL |
| updated_at | TIMESTAMPTZ | NOT NULL, auto-update |

---

### 6. contact_submissions

Public contact form entries.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK |
| name | VARCHAR(255) | NOT NULL |
| email | VARCHAR(255) | NOT NULL |
| subject | VARCHAR(255) | NOT NULL |
| message | TEXT | NOT NULL |
| is_read | BOOLEAN | NOT NULL, default false |
| created_at | TIMESTAMPTZ | NOT NULL, indexed |

---

### 7. newsletter_subscribers

Newsletter opt-in list.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK |
| email | VARCHAR(255) | UNIQUE, NOT NULL, indexed |
| is_active | BOOLEAN | NOT NULL, default true |
| subscribed_at | TIMESTAMPTZ | NOT NULL, indexed |

---

### 8. agent_types

Predefined and custom chat agent definitions.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK |
| name | VARCHAR(120) | NOT NULL |
| slug | VARCHAR(120) | UNIQUE, NOT NULL, indexed |
| description | TEXT | nullable |
| icon | VARCHAR(120) | nullable (Lucide icon name) |
| sort_order | INTEGER | NOT NULL, default 0, indexed |
| system_prompt | TEXT | NOT NULL |
| tools_config | JSONB | NOT NULL, default [] (allowed RAG section types) |
| is_system | BOOLEAN | NOT NULL, default true, indexed |
| workspace_id | UUID | nullable, indexed (null = system agent) |
| created_by | UUID | nullable, indexed (Supabase user id, no FK) |
| created_at | TIMESTAMPTZ | NOT NULL, indexed |
| updated_at | TIMESTAMPTZ | NOT NULL, auto-update |

**Relationships**: `agent_types` 1--N `chat_conversations`

---

### 9. chat_conversations

Conversation threads scoped to a workspace + audit + agent.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK |
| workspace_id | UUID | NOT NULL, indexed |
| audit_id | UUID | FK -> audits.id, NOT NULL, indexed |
| created_by | UUID | NOT NULL, indexed (Supabase user id, no FK) |
| agent_type_id | UUID | FK -> agent_types.id, NOT NULL, indexed |
| title | VARCHAR(300) | nullable |
| is_shared | BOOLEAN | NOT NULL, default false, indexed |
| verbosity | VARCHAR(20) | NOT NULL, default 'balanced' (concise/balanced/detailed) |
| tone | VARCHAR(20) | NOT NULL, default 'professional' (technical/professional/simple) |
| created_at | TIMESTAMPTZ | NOT NULL, indexed |
| updated_at | TIMESTAMPTZ | NOT NULL, auto-update |

**Relationships**: 1--N `chat_messages` (cascade), 1--N `chat_shares` (cascade)

---

### 10. chat_messages

Individual messages inside a conversation.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK |
| conversation_id | UUID | FK -> chat_conversations.id (CASCADE), NOT NULL, indexed |
| role | ENUM (user/assistant/system) | NOT NULL, indexed |
| content | TEXT | NOT NULL |
| tokens_used | INTEGER | nullable |
| created_at | TIMESTAMPTZ | NOT NULL, indexed |

**Relationships**: 1--N `chat_attachments`

---

### 11. chat_attachments

Files uploaded to a conversation, stored on VPS volume.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK |
| conversation_id | UUID | FK -> chat_conversations.id (CASCADE), NOT NULL, indexed |
| message_id | UUID | FK -> chat_messages.id (SET NULL), nullable, indexed |
| workspace_id | UUID | NOT NULL, indexed |
| uploaded_by | UUID | NOT NULL, indexed (Supabase user id) |
| filename | TEXT | NOT NULL |
| mime_type | VARCHAR(200) | NOT NULL |
| size_bytes | INTEGER | NOT NULL |
| storage_path | TEXT | NOT NULL |
| created_at | TIMESTAMPTZ | NOT NULL, indexed |

---

### 12. chat_message_feedback

Thumbs up/down ratings on assistant messages.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK |
| message_id | UUID | FK -> chat_messages.id (CASCADE), NOT NULL, indexed |
| user_id | UUID | NOT NULL, indexed (Supabase user id) |
| rating | INTEGER | NOT NULL (+1 or -1) |
| created_at | TIMESTAMPTZ | NOT NULL, indexed |

**Unique**: `(message_id, user_id)`

---

### 13. chat_shares

Share a conversation with another workspace member.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK |
| conversation_id | UUID | FK -> chat_conversations.id (CASCADE), NOT NULL, indexed |
| shared_with_user_id | UUID | NOT NULL, indexed |
| shared_by_user_id | UUID | NOT NULL, indexed |
| permission | ENUM (read/write) | NOT NULL, default 'read', indexed |
| created_at | TIMESTAMPTZ | NOT NULL, indexed |

**Unique**: `(conversation_id, shared_with_user_id)`

---

### 14. chat_usage

Monthly message counters for rate limiting.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK |
| user_id | UUID | NOT NULL, indexed (Supabase user id) |
| month | VARCHAR(7) | NOT NULL, indexed (YYYY-MM) |
| messages_sent | INTEGER | NOT NULL, default 0 |
| updated_at | TIMESTAMPTZ | NOT NULL, auto-update |

**Unique**: `(user_id, month)`

### 10. credit_balances

Per-workspace credit balance. One row per workspace. Uses SELECT FOR UPDATE for atomic deductions.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK, default uuid4 |
| workspace_id | UUID | UNIQUE, NOT NULL, indexed |
| subscription_credits | INTEGER | NOT NULL, default 0 |
| purchased_credits | INTEGER | NOT NULL, default 0 |
| created_at | TIMESTAMPTZ | NOT NULL, server default now() |
| updated_at | TIMESTAMPTZ | NOT NULL, auto-update |

**Total balance** = subscription_credits + purchased_credits. Deductions consume subscription first, then purchased.

### 11. credit_transactions

Append-only credit transaction ledger. Never delete rows.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK, default uuid4 |
| workspace_id | UUID | NOT NULL, indexed |
| user_id | UUID | NOT NULL |
| type | VARCHAR(30) | NOT NULL, indexed (grant_subscription, grant_purchase, deduct_audit, deduct_chat, refund, admin_adjustment) |
| amount | INTEGER | NOT NULL (positive=credit, negative=debit) |
| balance_after | INTEGER | NOT NULL |
| metadata | JSONB | nullable (audit_id, stripe_session_id, package_name, etc.) |
| created_at | TIMESTAMPTZ | NOT NULL, server default now(), indexed |

**Indexes**: `(workspace_id, created_at DESC)`, `(type)`

---

## Supabase PostgreSQL Tables

Defined in `supabase/schema.sql`. All tables are in the `public` schema with Row Level Security (RLS).

### 1. profiles

Extends Supabase `auth.users` -- auto-created on signup via trigger.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK, FK -> auth.users(id) CASCADE |
| full_name | TEXT | nullable |
| avatar_url | TEXT | nullable |
| is_super_admin | BOOLEAN | default false |
| created_at | TIMESTAMPTZ | default now() |
| updated_at | TIMESTAMPTZ | default now(), trigger-updated |

---

### 2. workspaces

Personal or team workspace -- auto-created for every new user.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK |
| name | TEXT | NOT NULL |
| slug | TEXT | UNIQUE, NOT NULL, indexed |
| type | TEXT | NOT NULL, CHECK (personal/team) |
| owner_id | UUID | FK -> auth.users(id) CASCADE, indexed |
| created_at | TIMESTAMPTZ | default now() |
| updated_at | TIMESTAMPTZ | default now(), trigger-updated |

---

### 3. workspace_members

Junction table for workspace membership.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK |
| workspace_id | UUID | FK -> workspaces(id) CASCADE, indexed |
| user_id | UUID | FK -> auth.users(id) CASCADE, indexed |
| role | TEXT | NOT NULL, CHECK (owner/admin/member) |
| created_at | TIMESTAMPTZ | default now() |

**Unique**: `(workspace_id, user_id)`

---

### 4. invites

Pending workspace invitations with expiring tokens.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK |
| workspace_id | UUID | FK -> workspaces(id) CASCADE |
| email | TEXT | NOT NULL, indexed |
| role | TEXT | NOT NULL, CHECK (admin/member) |
| invited_by | UUID | FK -> auth.users(id) |
| token | TEXT | UNIQUE, NOT NULL, indexed |
| expires_at | TIMESTAMPTZ | NOT NULL |
| accepted_at | TIMESTAMPTZ | nullable |
| created_at | TIMESTAMPTZ | default now() |

---

### 5. subscriptions

Stripe subscription data, one per workspace.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK |
| workspace_id | UUID | FK -> workspaces(id) CASCADE, UNIQUE, indexed |
| stripe_customer_id | TEXT | UNIQUE |
| stripe_subscription_id | TEXT | UNIQUE |
| plan | TEXT | NOT NULL, CHECK (free/pro/enterprise), default 'free' |
| status | TEXT | NOT NULL, CHECK (active/canceled/past_due/trialing), default 'active' |
| current_period_start | TIMESTAMPTZ | nullable |
| current_period_end | TIMESTAMPTZ | nullable |
| cancel_at | TIMESTAMPTZ | nullable |
| audit_limit | INTEGER | NOT NULL, default 5 |
| audits_used_this_month | INTEGER | NOT NULL, default 0 |
| created_at | TIMESTAMPTZ | default now() |
| updated_at | TIMESTAMPTZ | default now(), trigger-updated |

---

### 6. invoices

Stripe invoice history.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK |
| workspace_id | UUID | FK -> workspaces(id) CASCADE |
| stripe_invoice_id | TEXT | UNIQUE, NOT NULL |
| amount_paid | INTEGER | NOT NULL (cents) |
| currency | TEXT | NOT NULL, default 'usd' |
| status | TEXT | NOT NULL |
| invoice_pdf | TEXT | nullable |
| created_at | TIMESTAMPTZ | default now() |

---

### 7. projects

One website per workspace. Referenced by VPS `audits.project_id` and `audit_schedules.project_id`.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK |
| workspace_id | UUID | FK -> workspaces(id) CASCADE, NOT NULL, indexed |
| name | TEXT | NOT NULL |
| url | TEXT | NOT NULL, indexed |
| description | TEXT | nullable |
| created_by | UUID | FK -> auth.users(id) |
| created_at | TIMESTAMPTZ | default now() |
| updated_at | TIMESTAMPTZ | default now(), trigger-updated |

---

### 8. project_members

Project-level team roles.

| Column | Type | Constraints |
|---|---|---|
| id | UUID | PK |
| project_id | UUID | FK -> projects(id) CASCADE, NOT NULL, indexed |
| user_id | UUID | FK -> auth.users(id) CASCADE, NOT NULL, indexed |
| role | TEXT | NOT NULL, CHECK (manager/member/viewer) |
| created_at | TIMESTAMPTZ | default now() |

**Unique**: `(project_id, user_id)`

### Supabase Triggers

- `update_updated_at_column()` -- auto-updates `updated_at` on profiles, workspaces, subscriptions, projects.
- `handle_new_user()` -- on `auth.users` INSERT: creates profile, personal workspace, owner membership, and free subscription.

---

## Relationships Diagram

```
VPS PostgreSQL                          Supabase PostgreSQL
--------------                          -------------------
users 1──N audits 1──N competitors      auth.users 1──1 profiles
                  1──N audit_tasks                 1──N workspaces (via owner_id)
                                                        1──N workspace_members
audit_schedules (FK user_id)                            1──1 subscriptions
                                                        1──N invoices
agent_types 1──N chat_conversations                     1──N projects
                 1──N chat_messages                          1──N project_members
                 1──N chat_shares

Cross-database (UUID, no FK):
  audits.workspace_id        --> workspaces.id
  audits.project_id          --> projects.id
  audit_schedules.project_id --> projects.id
  chat_conversations.workspace_id --> workspaces.id
  chat_conversations.created_by   --> auth.users.id
  agent_types.workspace_id        --> workspaces.id
  agent_types.created_by          --> auth.users.id
```

**Cascade rules (VPS)**:
- Delete user -> delete audits -> delete competitors + audit_tasks
- Delete audit -> delete competitors + audit_tasks
- Delete conversation -> delete messages + shares + attachments
- Delete message -> attachment.message_id set NULL

---

## JSONB Structures

### audits.results

```json
{
  "technical_health_index": {
    "score": 78.4,
    "grade": "B",
    "status": "moderate",
    "breakdown": {
      "lighthouse_pillar": 81.0,
      "crawl_health_pillar": 74.2,
      "tech_extras_pillar": 79.3,
      "content_pillar": 76.5,
      "security_pillar": 83.0
    }
  },
  "visibility_momentum": {
    "score": 22.3,
    "status": "growing",
    "wins_count": 1820,
    "losses_count": 930
  },
  "traffic_estimation": {
    "total_estimated_monthly": 12840,
    "potential_monthly": 16420,
    "potential_gain": 3580,
    "keyword_count": 10000,
    "by_position_bracket": {
      "top3": {"label": "TOP 3", "keywords": 140, "estimated_traffic": 5860.2},
      "top4_10": {"label": "TOP 4-10", "keywords": 470, "estimated_traffic": 4020.4},
      "top11_20": {"label": "TOP 11-20", "keywords": 910, "estimated_traffic": 1780.8}
    },
    "top_traffic_keywords": [],
    "top_traffic_urls": [],
    "top_opportunities": []
  },
  "content_quality_index": {
    "site_score": 73.4,
    "grade": "C",
    "distribution": {"A": 18, "B": 42, "C": 63, "D": 27, "F": 9},
    "pages_count": 159,
    "top_issues": [{"issue": "thin_content", "count": 45}],
    "components": {
      "word_count": 70.4, "text_ratio": 74.8, "title_quality": 78.1,
      "meta_quality": 66.5, "h1_quality": 81.2, "readability": 69.7,
      "internal_linking": 72.9, "crawl_depth": 79.0
    },
    "pages": []
  },
  "crawl": {
    "title": "string", "title_length": 0,
    "meta_description": "string", "meta_description_length": 0,
    "h1_tags": [], "h1_count": 0, "status_code": 200,
    "word_count": 0, "size_bytes": 0, "load_time": 0.0,
    "total_images": 0, "images_without_alt": 0,
    "internal_links_count": 0, "has_sitemap": true,
    "ai_readiness": {
      "score": 68.2, "status": "partial",
      "checks": [], "recommendations": [],
      "training_bots": {"blocked": [], "allowed": [], "unknown": []},
      "citation_bots": {"blocked": [], "allowed": [], "unknown": []},
      "llms_txt": {"exists": false, "valid": false}
    }
  },
  "lighthouse": {
    "desktop": {
      "url": "string", "device": "desktop",
      "performance_score": 85, "accessibility_score": 92,
      "best_practices_score": 88, "seo_score": 95,
      "ttfb": 450, "fcp": 1200, "lcp": 2100, "cls": 0.05,
      "total_blocking_time": 150, "speed_index": 1800, "status_code": 200
    },
    "mobile": { "/* same structure */" }
  },
  "content_analysis": {
    "quality_score": 85, "readability_score": 75,
    "recommendations": [], "word_count": 850,
    "has_title": true, "has_meta_description": true, "has_h1": true
  },
  "local_seo": {
    "is_local_business": true, "has_nap": false,
    "has_schema_markup": false, "recommendations": []
  },
  "performance_analysis": {
    "issues": [], "recommendations": [],
    "ttfb_desktop": 450, "lcp_desktop": 2100,
    "performance_score": 85, "impact": "low"
  },
  "competitive_analysis": {
    "strengths": [], "weaknesses": [],
    "opportunities": [], "recommendations": [],
    "competitors_analyzed": 3
  }
}
```

### competitors.results

```json
{
  "lighthouse": {
    "url": "string", "device": "desktop",
    "performance_score": 75, "accessibility_score": 90,
    "best_practices_score": 85, "seo_score": 92
  }
}
```

### audit_tasks.fix_data

```json
{
  "current_value": "Missing alt on 12 images",
  "suggested_value": "Add descriptive alt text to all images",
  "code_snippet": "<img src=\"hero.jpg\" alt=\"Product dashboard overview\">"
}
```

---

## Migrations

**Tool**: Alembic
**Location**: `backend/alembic/versions/`
**Total**: 17 migration files

| # | File | Description |
|---|---|---|
| 1 | `20251205_0925_30ef0ad04684_initial_migration_users_audits_.py` | Create users, audits, competitors tables + indexes |
| 2 | `20260211_0100_add_public_tables.py` | Add contact_submissions, newsletter_subscribers |
| 3 | `20260211_ai_pipeline_toggle.py` | Add run_ai_pipeline column to audits |
| 4 | `20260211_senuto_fields.py` | Add senuto_country_id, senuto_fetch_mode to audits |
| 5 | `20260214_add_audit_tasks_table.py` | Create audit_tasks table |
| 6 | `20260214_fix_competitors_cascade.py` | Fix cascade delete on competitors FK |
| 7 | `20260214_make_audits_user_id_nullable.py` | Make audits.user_id nullable for workspace-based audits |
| 8 | `20260214_missing_columns_and_schedules.py` | Add missing audit columns + audit_schedules table |
| 9 | `20260215_chat_tables.py` | Create agent_types, chat_conversations, chat_messages, chat_shares, chat_usage |
| 10 | `20260215_update_agent_tools_config.py` | Update agent tools_config defaults |
| 11 | `20260216_add_rag_indexed_at.py` | Add rag_indexed_at column to audits |
| 12 | `20260216_agent_sort_order.py` | Add sort_order to agent_types |
| 13 | `20260216_chat_attachments.py` | Create chat_attachments table |
| 14 | `20260216_chat_conversation_style.py` | Add verbosity, tone to chat_conversations |
| 15 | `20260216_chat_feedback.py` | Create chat_message_feedback table |
| 16 | `20260217_add_crawler_ua_and_crawl_blocked.py` | Add crawler_user_agent, crawl_blocked to audits |
| 17 | `20260217_add_project_id.py` | Add project_id to audits + audit_schedules |

### Running Migrations

```bash
# Apply all pending
docker exec sitespector-backend alembic upgrade head

# Create new migration from model changes
docker exec sitespector-backend alembic revision --autogenerate -m "description"

# Rollback one step
docker exec sitespector-backend alembic downgrade -1

# View migration history
docker exec sitespector-backend alembic history
```

---

## Common Queries

### SQL

```sql
-- Recent audits for a workspace
SELECT id, url, status, overall_score, created_at
FROM audits
WHERE workspace_id = '<uuid>' AND project_id = '<uuid>'
ORDER BY created_at DESC
LIMIT 20;

-- Pending audits (worker queue)
SELECT id, url, user_id, workspace_id
FROM audits
WHERE status = 'pending'
ORDER BY created_at ASC
LIMIT 3;

-- Timed-out processing audits
SELECT id, url, started_at
FROM audits
WHERE status = 'processing'
  AND started_at < NOW() - INTERVAL '10 minutes';

-- Audit with competitors
SELECT a.*, json_agg(c.*) AS competitors
FROM audits a
LEFT JOIN competitors c ON c.audit_id = a.id
WHERE a.id = '<uuid>'
GROUP BY a.id;

-- Aggregate scores
SELECT
    AVG(overall_score) AS avg_overall,
    AVG(seo_score) AS avg_seo,
    AVG(performance_score) AS avg_performance,
    AVG(content_score) AS avg_content
FROM audits
WHERE status = 'completed';

-- Quick-win tasks for an audit
SELECT id, module, title, priority, impact, effort
FROM audit_tasks
WHERE audit_id = '<uuid>' AND is_quick_win = true
ORDER BY sort_order;
```

### SQLAlchemy (async)

```python
# Get pending audits
result = await db.execute(
    select(Audit)
    .where(Audit.status == AuditStatus.PENDING)
    .order_by(Audit.created_at)
    .limit(3)
)
pending = result.scalars().all()

# Audit with competitors (eager load)
result = await db.execute(
    select(Audit)
    .options(selectinload(Audit.competitors))
    .where(Audit.id == audit_id)
)
audit = result.scalar_one()

# Update status
audit.status = AuditStatus.PROCESSING
audit.started_at = datetime.utcnow()
await db.commit()
```

---

## Database Access

```bash
# Connect to VPS Postgres
docker exec -it sitespector-postgres psql -U sitespector_user -d sitespector_db

# Useful psql commands
\dt                  -- list tables
\d audits            -- describe table
\di                  -- list indexes
SELECT pg_size_pretty(pg_database_size('sitespector_db'));
```

---

## Backup & Restore

### VPS PostgreSQL

```bash
# Full backup
docker exec sitespector-postgres pg_dump -U sitespector_user sitespector_db > backup.sql

# Compressed backup
docker exec sitespector-postgres pg_dump -U sitespector_user sitespector_db | gzip > backup.sql.gz

# Schema only
docker exec sitespector-postgres pg_dump -U sitespector_user sitespector_db --schema-only > schema.sql

# Restore from SQL
cat backup.sql | docker exec -i sitespector-postgres psql -U sitespector_user sitespector_db

# Restore from compressed
gunzip -c backup.sql.gz | docker exec -i sitespector-postgres psql -U sitespector_user sitespector_db
```

### Supabase

Supabase provides automatic daily backups via the dashboard. For manual export, use `pg_dump` through the Supabase connection string or the Supabase CLI (`supabase db dump`).

---

## Operational Runbook: Baseline Reset

When testing requires a clean slate after model changes:

1. **VPS Postgres** -- truncate runtime tables:
   ```sql
   TRUNCATE audits, competitors, audit_tasks, audit_schedules,
            chat_conversations, chat_messages, chat_attachments,
            chat_message_feedback, chat_shares, chat_usage CASCADE;
   UPDATE users SET audits_count = 0;
   ```
2. **Supabase** -- clear project data and reset counters:
   ```sql
   DELETE FROM project_members;
   DELETE FROM projects;
   UPDATE subscriptions SET audits_used_this_month = 0;
   ```
3. **Verify** -- `count(*) = 0` on all reset tables; no non-zero counters (`users.audits_count`, `subscriptions.audits_used_this_month`).

This preserves schema, triggers, RLS policies, users, profiles, workspaces, and memberships.

---

**Last Updated**: 2026-03-17
**VPS Database**: PostgreSQL 16 (Docker)
**Supabase**: Managed PostgreSQL
**ORM**: SQLAlchemy 2.0 (async) + asyncpg
**Migrations**: Alembic (17 files)
