["feature-name] implementation\"\n\n# Verify API contracts\nquery-docs", "API endpoints [feature]\"\n\n# Check decisions\nquery-docs", "decision about [topic]", "Location**: `.context7/` directory\n- `project/` - Overview, architecture, stack, deployment\n- `backend/` - API, models, worker, AI services  \n- `frontend/` - Components, pages, API client\n- `infrastructure/` - Docker, nginx, database\n- `decisions/` - Decision log, bugs & fixes\n\n### After Completing ANY Task\n\n**ALWAYS Update Context7:**\n1. Update relevant `.context7/` files with changes\n2. Add architectural decisions to `decisions/DECISIONS_LOG.md`\n3. Document bugs/fixes in `decisions/BUGS_AND_FIXES.md`\n4. Keep documentation concise and scannable\n\n---\n\n## Project Overview\n\n**Status**: Production-ready with Teams, Billing, Workspaces  \n**Stack**: Next.js 14, FastAPI, Supabase, PostgreSQL (dual-DB), Docker  \n**VPS**: 77.42.79.46 (Hetzner, 8GB RAM)\n\n### Quick Architecture\n\n**Dual Database Strategy:**\n- **Supabase**: Users, Teams, Workspaces, Subscriptions (with RLS)\n- **VPS PostgreSQL**: Audits, Results, Competitors (high-volume)\n\n**Frontend**: Next.js 14 App Router with (app) route group for sidebar layout  \n**Backend**: FastAPI with Supabase JWT verification  \n**Auth**: Supabase Auth (email/password, OAuth ready, magic links)  \n**Billing**: Stripe (Free/Pro/Enterprise tiers with usage limits)\n\n---\n\n## Development Workflow\n\n### Local Development\n1. Edit code locally in KiloCode\n2. Test changes if needed\n3. Commit: `git add . && git commit -m \"feat: description", 4.0, "ASK before push** (never auto-push)\n\n### VPS Deployment\n```bash\n# After user approves push\nssh root@77.42.79.46\ncd /opt/sitespector\ngit pull origin release\n\n# Backend/Worker changes (restart only)\ndocker compose -f docker-compose.prod.yml restart backend worker\n\n# Frontend changes (REBUILD required)\ndocker compose -f docker-compose.prod.yml build --no-cache frontend\ndocker compose -f docker-compose.prod.yml up -d frontend\n\n# Monitor logs\ndocker logs sitespector-frontend --tail 50 -f\n```\n\n---\n\n## Code Patterns\n\n### TypeScript (Frontend)\n```typescript\n// UUID is always string from PostgreSQL\ninterface Audit {\n  id: string // Not UUID type!\n  workspace_id: string\n}\n\n// Always use optional chaining & nullish coalescing\nconst score = audit.overall_score ?? 0\nconst title = audit.results?.crawl?.title ?? 'No title'\n\n// React Query for data fetching\nconst { data, isLoading, isError } = useQuery({\n  queryKey: ['audits', workspaceId],\n  queryFn: () => auditsAPI.list(workspaceId),\n  enabled: !!workspaceId\n})\n```\n\n### Python (Backend)\n```python\n# Always async for DB and external calls\nasync def get_audit(audit_id: str, db: AsyncSession) -> Audit:\n    result = await db.execute(\n        select(Audit).where(Audit.id == audit_id)\n    )\n    return result.scalar_one_or_none()\n\n# Use FastAPI HTTPException\nfrom fastapi import HTTPException, status\n\nif not audit:\n    raise HTTPException(\n        status_code=status.HTTP_404_NOT_FOUND,\n        detail=f\"Audit {audit_id} not found", "Supabase RLS\n```sql\n-- Simple, direct policies (avoid recursion)\nCREATE POLICY \"policy_name", "ON public.table_name\nFOR SELECT\nTO authenticated\nUSING (\n  user_id = auth.uid()  -- Direct comparison, no subqueries\n);\n```\n\n---\n\n## Key Technologies\n\n**Frontend Stack:**\n- Next.js 14 (App Router, route groups, server/client components)\n- TypeScript (strict mode)\n- Tailwind CSS + shadcn/ui\n- TanStack Query (React Query)\n- Supabase JS Client\n- next-themes (dark mode)\n\n**Backend Stack:**\n- FastAPI (Python 3.11, async/await)\n- SQLAlchemy 2.0 (async)\n- Supabase Auth (JWT verification)\n- Stripe SDK (payments)\n- Google Gemini API (AI analysis)\n- WeasyPrint (PDF generation)\n\n**Infrastructure:**\n- Docker Compose (7 containers)\n- Nginx (reverse proxy, SSL)\n- Hetzner VPS\n\n---\n\n## Common Tasks\n\n### Add New Supabase Table\n1. Create table in Supabase SQL Editor\n2. Add RLS policies (simple, direct)\n3. Update `.context7/infrastructure/DATABASE.md`\n4. Add to TypeScript types if needed\n\n### Add New API Endpoint\n1. Check `.context7/backend/API.md` for patterns\n2. Add route in `backend/app/routers/`\n3. Use `get_current_user` dependency\n4. Verify workspace access if needed\n5. Update `.context7/backend/API.md`\n\n### Add New Frontend Page\n1. Create in `app/(app)/` for sidebar layout\n2. Use 'use client' for client components\n3. Use React Query for data fetching\n4. Use workspace context: `useWorkspace()`\n5. Update `.context7/frontend/PAGES.md`\n\n### Debug Issues\n1. Check `.context7/decisions/BUGS_AND_FIXES.md` first\n2. Frontend: `docker logs sitespector-frontend`\n3. Backend: `docker logs sitespector-backend`\n4. Database: Check Supabase Dashboard -> Logs\n\n---\n\n## Environment Variables\n\n**Local** (`.env` in workspace root):\n- Development only, not used in deployment\n- Keep as reference for structure\n\n**Production** (`/opt/sitespector/.env` on VPS):\n- Contains all secrets (Supabase, Stripe, Gemini, JWT)\n- Never commit to Git\n- Access only via SSH\n\n**Frontend Build** (Docker build args):\n- `NEXT_PUBLIC_API_URL`\n- `NEXT_PUBLIC_SUPABASE_URL`\n- `NEXT_PUBLIC_SUPABASE_ANON_KEY`\n- Set in `docker-compose.prod.yml`\n\n---\n\n## Git Workflow\n\n**Branch**: `release` (main production branch)\n\n**Commit Format**:\n```bash\nfeat: add feature\nfix: bug fix\ndocs: documentation\nrefactor: code refactoring\ntest: add tests\nchore: maintenance\n```\n\n**Rules**:\n- Auto-commit: ALLOWED\n- Auto-push: FORBIDDEN (always ask first)\n- Push after approval: `git push origin release`\n\n---\n\n## Quick Reference\n\n**Access**:\n- VPS: `ssh root@77.42.79.46`\n- App: `https://77.42.79.46`\n- Test User: `info@craftweb.pl` / `Dawid132?`\n\n**Docker Commands**:\n```bash\ndocker ps                           # Check containers\ndocker logs sitespector-frontend    # View logs\ndocker exec -it sitespector-backend bash  # Shell access\n```\n\n**Database Access**:\n```bash\n# VPS PostgreSQL\ndocker exec -it sitespector-postgres psql -U sitespector_user -d sitespector_db\n\n# Supabase\n# Use Supabase Dashboard SQL Editor\n```\n\n---\n\n## Migration Notes\n\n**Migrated from Cursor to KiloCode** (2026-02-04)\n\n**Archived**:\n- Old Cursor plans -> `.archive/old-plans/`\n- Temporary documentation -> `.archive/temp-docs/`\n- SQL fix files -> `.archive/sql-fixes/`\n\n**Kept**:\n- `.context7/` - Complete technical documentation\n- `.cursor/rules/` - Personal preferences (for reference)\n- `docs/` - Deployment guides and checklists\n\n---\n\n**For detailed documentation, ALWAYS check `.context7/` files first!**"]